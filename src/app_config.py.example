# src/app_config.py
from typing import Dict

# 对话配置
DIALOGUE_CONFIG = {
    "max_turns": 100,  # 最大对话轮次
    "timeout": 500,   # 对话超时时间(秒)
    "min_confidence": 0.7  # 最小置信度阈值
}

# LLM配置
LLM_CONFIG = {
    "temperature": 0.1,
    "max_tokens": 500,
    "model": "",
    "api_key": "",
    "base_url": ""
}